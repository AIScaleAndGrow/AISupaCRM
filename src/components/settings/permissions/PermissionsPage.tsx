import { PlaceholderContent } from "../shared/PlaceholderContent";

export function PermissionsPage() {
  return (
    <PlaceholderContent
      title="Permissions"
      description="Configure user roles and access permissions for your organization."
    />
  );
}
