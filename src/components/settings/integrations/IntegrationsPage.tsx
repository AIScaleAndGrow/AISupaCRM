import { PlaceholderContent } from "../shared/PlaceholderContent";

export function IntegrationsPage() {
  return (
    <PlaceholderContent
      title="Integrations"
      description="Connect and manage third-party integrations and services."
    />
  );
}
